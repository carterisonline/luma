{
  "name": "stackbin-compiler",
  "version": "0.1.0",
  "description": "The STACKBIN language compiler to IR",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p build server",
    "build": "tsc --watch",
    "server": "browser-sync start --server -w",
    "start-debug": "./run-tsr.sh",
    "build-debug": "./build-tsr.sh",
    "build-legacy": "tsc && ./build-legacy.sh"
  },
  "homepage": "https://github.com/carterisonline/stackbin-compiler",
  "devDependencies": {
    "@types/p5": "^0.9.1",
    "browser-sync": "^2.26.12",
    "npm-run-all": "^4.1.5",
    "ts-runtime": "^0.3",
    "typescript": "^4.2.1-rc"
  },
  "dependencies": {
    "p5": "^1.3.1"
  }
}
